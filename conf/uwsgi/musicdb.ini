[uwsgi]
workers = 1
master = true
auto-procname = true
procname-prefix-spaced = %n

module = musicdb.main
callable = app

socket = /srv/%n/uwsgi-%n.sock
chmod-socket = 666

chdir = /srv/%n/web
virtualenv = /srv/%n

uid=joost
gid=joost
